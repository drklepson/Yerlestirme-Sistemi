import 'package:flutter/material.dart';

const boxConstraints450 = BoxConstraints(maxWidth: 450);
const edgeInsetsH10 = EdgeInsets.symmetric(horizontal: 10);
const radius20 = Radius.circular(20);
const borderRadiusTop20 = BorderRadius.vertical(top: radius20);
const borderRadiusBottom20 = BorderRadius.vertical(bottom: radius20);
